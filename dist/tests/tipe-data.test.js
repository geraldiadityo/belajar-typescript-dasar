"use strict";
describe('Data Type', function () {
    it('should be declare', function () {
        const name = 'geraldi adityo';
        const balance = 1000000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
