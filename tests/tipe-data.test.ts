describe('Data Type', function() {
    it('should be declare', function() {
        
        const name: string = 'geraldi adityo';
        const balance: number = 1000000;
        const isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
